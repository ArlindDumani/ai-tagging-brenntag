# Google Gemini API Key (https://aistudio.google.com/apikey)
#
# Diese Datei ist nur eine VORLAGE. So richtest du deine echte .env ein:
#   1. Diese Datei kopieren und " .env " nennen (ohne "example"):
#        copy .env.example .env
#   2. In der Datei .env den Platzhalter durch deinen echten API-Key ersetzen.
# Die Datei .env wird nicht ins Repo hochgeladen.
#
GEMINI_API_KEY=dein-api-key-hier
